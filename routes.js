const HOME = '/'
const JOIN = '/join'
const LOGIN = '/log-in'
const LOGOUT = '/log-out'
const SEARCH = '/search'

const routes = {
    home : HOME,
    join : JOIN,
    login : LOGIN,
    logout : LOGOUT,
    search : SEARCH,
}

export default routes;